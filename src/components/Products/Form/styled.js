import styled from "styled-components";

const StyledForm = styled.form`

    margin-top: 30px;
    .checkout__input{
        display: flex;
        flex-direction: column;
        padding: 5px;
    }

`;

export {StyledForm}